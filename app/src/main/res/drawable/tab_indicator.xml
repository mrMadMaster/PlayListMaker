<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Прозрачная область для отступов -->
    <item
        android:left="16dp"
        android:right="16dp"
        android:top="0dp"
        android:bottom="0dp">

        <!-- Сам индикатор -->
        <shape android:shape="rectangle">
            <solid android:color="@color/text" />
            <size android:height="2dp" />
        </shape>
    </item>
</layer-list>